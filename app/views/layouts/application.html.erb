<!DOCTYPE html>
<html class="no-js" lang="fr">
 <head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= content_for?(:title) ? yield(:title) : "Paps" %></title>
  <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Paps" %>">
  <%= stylesheet_link_tag    'application', :media=> 'all' %>
  <%= javascript_include_tag 'application' %>
  <%# Modernizr is required for Zurb Foundation %>
  <%= javascript_include_tag 'vendor/modernizr' %>
  <%= csrf_meta_tags %>
 </head>
 <body>
  <header>
   <%= render 'layouts/navigation' %>
  </header>
 <div class="container-fluid">
  <% if user_signed_in? %>
  <div class="row">
   <div class="col-sm-12 text-right signedin">
    Connecté en tant que <%= current_user.email %>
    <% if current_user.admin? %>
      (<strong>Admin</strong>)
    <% else %>
      (<strong><%= current_user.bar %></strong>)
    <% end %>
   </div>
  </div>
  <% end %>
  </div>
  <main role="main" class="container">
     <%= render 'layouts/messages' %>
     <%= yield %>
     <%= debug(params) if Rails.env.development? %>
   </main>
 </body>
</html>
