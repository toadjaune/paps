<h1>Liste des bars</h1>

<%= link_to 'Retour', root_url %><br/>
<%= link_to 'Nouveau Bar', new_bar_path %>

<table>
  <tr>
    <th>Nom</th>
  </tr>

  <% @bars.each do |bar| %>
    <tr>
      <td><%= link_to bar, bar %></td>
    </tr>
  <% end %>
</table>
