<h1>Liste des PAPS</h1>

<%= link_to 'Retour', root_url %><br/>
<%= link_to 'Nouveau PAPS', new_pap_path %>

<table>
  <tr>
    <th>Bar</th>
    <th>Jour</th>
    <th>Question</th>
    <th>Réponse</th>
  </tr>

  <% @paps.each do |pap| %>
    <tr>
      <td><%= link_to pap.bar %></td>
      <td><%= link_to pap.jour %></td>
      <td><%= pap.question %></td>
      <td><%= link_to pap.reponse, pap %></td>
    </tr>
  <% end %>
</table>
